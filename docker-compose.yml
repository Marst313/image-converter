services:
  app:
    platform: linux/amd64
    image: marst313/image-converter:latest

    container_name: image-converter

    restart: unless-stopped

    ports:
      - "127.0.0.1:3001:3001"

    env_file:
      - .env
